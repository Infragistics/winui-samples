<Page
    x:Class="SamplesBrowser.Pages.SampleHostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource SamplePageBackground}">

    <!--
        This page hosts a sample UserControl.
        The UserControl instance is passed as a navigation parameter (Func<object>)
        and placed into SampleContent on navigation.
        Only the active sample is instantiated (lazy loading).
    -->
    <Border Padding="{StaticResource SampleContentPadding}">
        <ContentPresenter x:Name="SampleContent"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Stretch"/>
    </Border>

</Page>
