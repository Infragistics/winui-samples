<UserControl
    x:Class="SamplesBrowser.Pages.Charts.CategoryChart.Overview.Sample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Category Chart - Overview"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24">
            <StackPanel Spacing="12">
                <TextBlock Text="This sample demonstrates the Category Chart component."
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyTextBlockStyle}"/>
                <TextBlock Text="The Category Chart is a high-performance charting control that automatically analyzes and selects the best chart type for your data."
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyTextBlockStyle}"/>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>

