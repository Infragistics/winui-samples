<UserControl
    x:Class="SamplesBrowser.Pages.Gauges.RadialGauge.Overview.Sample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Radial Gauge - Overview"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24">
            <StackPanel Spacing="12">
                <TextBlock Text="This sample demonstrates the Radial Gauge component."
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyTextBlockStyle}"/>
                <TextBlock Text="The Radial Gauge displays a value within a visual element shaped like a clock face."
                           TextWrapping="Wrap"
                           Style="{StaticResource BodyTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Value:" VerticalAlignment="Center"/>
                    <Slider Minimum="0" Maximum="100" Value="65" Width="200"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>

