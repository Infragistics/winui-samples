<UserControl
    x:Class="Sample.Sample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ig="using:Infragistics.Controls"
        xmlns:igCharts="using:Infragistics.Controls.Charts"
                                mc:Ignorable="d">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <StackPanel>
                                      </StackPanel>
      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
                <Grid Grid.Column="1">
          <igCharts:XamDataChart
          Name="chart"
          ShouldAutoExpandMarginForInitialLabels="true"
          ComputedPlotAreaMarginMode="Series"
          IsVerticalZoomEnabled="false"
          IsHorizontalZoomEnabled="true"
          Brushes="green red"
          Outlines="green red"
          PlotAreaMarginLeft="10"
          PlotAreaMarginTop="0"
          PlotAreaMarginRight="20"
          PlotAreaMarginBottom="0"
          LeftMargin="10"
          TopMargin="0"
          RightMargin="20"
          BottomMargin="0"
          IsWindowSyncedToVisibleRange="false"
          Title="This Data Chart demonstrates the DataAnnotationRectLayer bound to data that annotates bearish patterns in stock prices.">
              <igCharts:XamDataChart.Axes>
                  <igCharts:CategoryXAxis
                  Name="xAxis"
                  ItemsSource="{Binding StockTesla}"
                  Label="Date"
                  LabelLeftMargin="0"
                  LabelTopMargin="10"
                  LabelRightMargin="0"
                  LabelBottomMargin="10">
                  </igCharts:CategoryXAxis>
                  <igCharts:NumericYAxis
                  Name="yAxis"
                  MinimumValue="0"
                  MaximumValue="550">
                  </igCharts:NumericYAxis>
              </igCharts:XamDataChart.Axes>
              <igCharts:XamDataChart.Series>
                  <igCharts:FinancialPriceSeries
                  Name="series1"
                  Title="Stock Price"
                  DisplayType="Candlestick"
                  XAxis="{Binding ElementName=xAxis}"
                  YAxis="{Binding ElementName=yAxis}"
                  ItemsSource="{Binding StockTesla}"
                  OpenMemberPath="Open"
                  HighMemberPath="High"
                  LowMemberPath="Low"
                  CloseMemberPath="Close"
                  ShowDefaultTooltip="false">
                  </igCharts:FinancialPriceSeries>
                  <igCharts:DataToolTipLayer
                  Name="Tooltip"
                  IncludedColumns="{Binding includedColumns1}"
                  LayoutMode="Vertical">
                  </igCharts:DataToolTipLayer>
                  <igCharts:DataAnnotationRectLayer
                  Name="RectLayer"
                  ItemsSource="{Binding AnnotationRectData}"
                  TargetAxis="{Binding ElementName=xAxis}"
                  CenterLabelXDisplayMode="Hidden"
                  StartLabelXDisplayMode="Hidden"
                  EndLabelXDisplayMode="Hidden"
                  StartLabelYDisplayMode="Hidden"
                  EndLabelYDisplayMode="Hidden"
                  ActualAreaFillOpacity="0.1"
                  Brush="purple"
                  Outline="purple"
                  OverlayTextColor="purple"
                  OverlayTextVerticalMargin="20"
                  OverlayTextHorizontalMargin="-50"
                  OverlayTextLocation="OutsideBottomCenter"
                  OverlayTextMemberPath="Label"
                  StartLabelXMemberPath="StartLabel"
                  EndLabelXMemberPath="EndLabel"
                  StartValueXMemberPath="StartX"
                  StartValueYMemberPath="StartY"
                  EndValueXMemberPath="EndX"
                  EndValueYMemberPath="EndY">
                  </igCharts:DataAnnotationRectLayer>
              </igCharts:XamDataChart.Series>
          </igCharts:XamDataChart>
        </Grid>
              </Grid>
          </Grid>
</UserControl>
